name: MKV Filter Metadata CI

on:
  push:
    branches: [ "main", "master" ]
    paths:
      - 'mkv-filter-metadata/**'
      - '.github/workflows/mkv-filter-metadata-ci.yml'
  pull_request:
    branches: [ "main", "master" ]
    paths:
      - 'mkv-filter-metadata/**'
      - '.github/workflows/mkv-filter-metadata-ci.yml'

jobs:
  lint:
    name: Lint & Check
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: mkv-filter-metadata
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Environment
        uses: ./.github/actions/mkv-filter-metadata-setup

      - name: Frontend check
        run: pnpm -F frontend check

      - name: Check for dead code
        run: pnpm run check:deadcode

      - name: Rust format check
        run: cargo fmt --manifest-path backend/Cargo.toml -- --check

      - name: Rust clippy
        run: cargo clippy --manifest-path backend/Cargo.toml -- -D warnings

  test:
    name: Test Coverage
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: mkv-filter-metadata
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Environment
        uses: ./.github/actions/mkv-filter-metadata-setup

      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@cargo-llvm-cov

      - name: Enforce test coverage
        run: pnpm run test:coverage

  build:
    name: Build Validation
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: mkv-filter-metadata
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Environment
        uses: ./.github/actions/mkv-filter-metadata-setup

      - name: Build Application
        run: CARGO_PROFILE_RELEASE_LTO=false pnpm exec tauri build --no-bundle

  test-windows:
    name: Test on Windows
    runs-on: windows-latest
    defaults:
      run:
        working-directory: mkv-filter-metadata
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Environment
        uses: ./.github/actions/mkv-filter-metadata-setup

      - name: Run tests
        run: pnpm test

  test-macos:
    name: Test on macOS
    strategy:
      matrix:
        os: [macos-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    env:
      RUST_BACKTRACE: "1"
    defaults:
      run:
        working-directory: mkv-filter-metadata
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Environment
        uses: ./.github/actions/mkv-filter-metadata-setup

      - name: Run tests
        run: |
          pnpm -F frontend test:unit --run
          cargo test --manifest-path backend/Cargo.toml -- --nocapture
